/** Stable ownership key for a personal Hardware installation. */
export const DEFAULT_LOCAL_OWNER_ID = "00000000-0000-4000-8000-000000000001";

/** Fixture data intentionally uses the same ownership shape as local mode. */
export const DEMO_ACTOR_ID = DEFAULT_LOCAL_OWNER_ID;
